# Known limits

The Windows client intentionally uses the same public-media backend as the website. It does not bypass platform authentication, private-account restrictions, DRM, removed posts, geo restrictions, or source-platform delivery changes. If a platform changes its public delivery mechanism, the backend adapter must be updated; the EXE does not embed a separate extractor.
