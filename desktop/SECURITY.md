# Security boundaries

The desktop app communicates only with `https://download.avocadoss.co.kr` and writes to a folder selected by the user. It does not collect browser cookies, credentials, or private-session data. Media access is limited to what the server-side public adapters can retrieve without bypassing source-platform access controls.
